<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Fizzly"
             x:Class="Fizzly.MainPage">
    <ContentPage.BindingContext>
        <local:MainViewModel></local:MainViewModel>
    </ContentPage.BindingContext>
	<Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Star"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Star"/>
        </Grid.ColumnDefinitions>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="Star" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Star" />
        </Grid.RowDefinitions>
        
        <StackLayout Grid.Column="1" Grid.Row="1">
            <Button AutomationId="FizzBuzzButton" Command="{Binding FizzBuzzCommand}">FizzBuzz</Button>
            <Label AutomationId="FizzBuzzLabel" Text="{Binding Value}"></Label>
        </StackLayout>
	</Grid>

</ContentPage>
